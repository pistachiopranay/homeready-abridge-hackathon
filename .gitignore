# Secrets — NEVER commit (repo is public)
.env
.env.*
*.pem
*.p12

# Pre-event spike work — not part of the hackathon build ("new work only" rule)
spikes-tonight/
vlog-shotlist.md

# Local working notes (contains venue creds / device ids)
CLAUDE.md

# macOS
.DS_Store

# Xcode / Swift
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.ipa
*.dSYM.zip

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Scratch
*.log

# Demo recordings (local footage, large)
demo_assets/

# Runtime walkthrough data (frames include personal home footage)
runs/

# Separate scratch app from another session — not part of the submission
relay-demo/

# Local agent notes (contains venue creds — never commit)
AGENTS.md
